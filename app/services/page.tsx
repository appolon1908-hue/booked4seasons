import Link from "next/link";
import { categories, servicesByCategory } from "@/lib/services";
import { ServiceCard } from "@/components/ServiceCard";
export const metadata={title:"Services",description:"Explore all 17 home, property, specialty, and mobile vehicle services available through Booked4Seasons.",alternates:{canonical:"/services"},openGraph:{title:"Services | Booked4Seasons",description:"Home services for every season.",url:"/services"}};
export default function Page(){return <><section className="page-hero"><div className="container narrow"><p className="eyebrow">Services</p><h1>Help for every part of the property — and every season.</h1><p className="lead">Explore our growing service catalog, then send one simple request when you’re ready.</p><div className="hero-actions"><Link className="button" href="/request-service">Request service</Link></div></div></section>{categories.map((category,index)=>{const id=category.name.toLowerCase().replaceAll(" ","-").replace("&-","");return <section id={id} className={`section ${index%2?"section-soft":""}`} key={category.name}><div className="container"><div className="section-heading"><div><p className="eyebrow">{category.name}</p><h2>{category.description}</h2></div></div><div className="service-grid">{servicesByCategory[category.name].map(service=><ServiceCard service={service} key={service.slug}/>)}</div></div></section>})}<section className="section section-dark"><div className="container cta"><div><p className="eyebrow">Don’t see exactly what you need?</p><h2>Tell us about the job.</h2><p>Our catalog can grow as new service categories and local coverage are added.</p></div><Link className="button" href="/contact">Contact us</Link></div></section></>}
