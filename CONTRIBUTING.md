# Contributing

1. Branch from `main`.
2. Keep changes focused and small enough to review.
3. Run `npm run typecheck`, `npx eslint .`, and `npm run build` before opening a PR.
4. Never commit secrets, customer data, payment credentials, or production tokens.
5. Use pull requests for changes to production-facing code.
