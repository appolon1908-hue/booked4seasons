import Link from "next/link";
import type { Service } from "@/lib/services";
export function ServicePage({service}:{service:Service}){return <><section className="page-hero"><div className="container narrow"><p className="eyebrow">{service.category} · {service.eyebrow}</p><h1>{service.name} service without the runaround.</h1><p className="lead">{service.description}</p><div className="hero-actions"><Link className="button" href={`/request-service?service=${service.slug}`}>Request {service.name}</Link><Link className="button button-secondary" href="/services">All services</Link></div></div></section><section className="section"><div className="container split"><div><p className="eyebrow">Common requests</p><h2>Tell us what needs attention.</h2></div><ul className="feature-list">{service.bullets.map(item=><li key={item}>{item}</li>)}</ul></div></section><section className="section section-soft"><div className="container"><p className="eyebrow">How it works</p><h2>A simple path from request to service.</h2><div className="steps large"><div><span>1</span><h3>Describe the job</h3><p>Choose {service.name} and share the important details.</p></div><div><span>2</span><h3>Share location & timing</h3><p>Tell us where service is needed and when works best.</p></div><div><span>3</span><h3>Get confirmation</h3><p>We confirm coverage and coordinate the appropriate next step.</p></div></div></div></section><section className="section"><div className="container cta"><div><p className="eyebrow">Need {service.name}?</p><h2>Get the request started.</h2><p>Send us the basics. No account required.</p></div><Link className="button" href={`/request-service?service=${service.slug}`}>Request service</Link></div></section></>}
