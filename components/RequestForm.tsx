"use client";
import { FormEvent, useState } from "react";
import { useSearchParams } from "next/navigation";
import { services } from "@/lib/services";
type Kind="service"|"contact"|"partner";
export function RequestForm({kind}:{kind:Kind}){const params=useSearchParams();const preset=params.get("service")||"";const [status,setStatus]=useState<"idle"|"sending"|"ok"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("sending");const form=e.currentTarget;const data=Object.fromEntries(new FormData(form).entries());const endpoint=process.env.NEXT_PUBLIC_FORM_ENDPOINT;if(!endpoint){setStatus("error");return}try{const res=await fetch(endpoint,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind,...data})});if(!res.ok)throw new Error();form.reset();setStatus("ok")}catch{setStatus("error")}}return <form className="request-form" onSubmit={submit}><div className="form-grid"><label>First name<input name="firstName" required autoComplete="given-name"/></label><label>Last name<input name="lastName" required autoComplete="family-name"/></label><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Phone<input name="phone" required autoComplete="tel"/></label>{kind!=="contact"&&<label className="span-2">Service<select name="service" defaultValue={preset} required><option value="">Choose a service</option>{services.map(s=><option key={s.slug} value={s.slug}>{s.name}</option>)}</select></label>}{kind==="service"&&<><label className="span-2">Service address<input name="address" required autoComplete="street-address"/></label><label>City<input name="city" required autoComplete="address-level2"/></label><label>ZIP / postal code<input name="postalCode" required autoComplete="postal-code"/></label><label>Preferred date<input name="preferredDate" type="date"/></label><label>Preferred time<select name="preferredTime"><option>Flexible</option><option>Morning</option><option>Afternoon</option><option>Evening</option></select></label></>}{kind==="partner"&&<><label className="span-2">Company name<input name="company" required/></label><label className="span-2">Cities / ZIP codes served<textarea name="areas" rows={3} required/></label></>}<label className="span-2">{kind==="partner"?"Tell us about your company":"Tell us what you need"}<textarea name="message" rows={5} required/></label></div><button className="button" type="submit" disabled={status==="sending"}>{status==="sending"?"Sending…":kind==="partner"?"Apply to become a provider":kind==="contact"?"Send message":"Request service"}</button>{status==="ok"&&<p className="form-success">Thanks — your request was received.</p>}{status==="error"&&<p className="form-error">Live form submission is not configured yet. Please contact Booked4Seasons directly before relying on this form.</p>}</form>}
